import os
from contextlib import contextmanager

import pytest


@pytest.fixture()
@contextmanager
def cwd(path):
    oldcwd = os.getcwd()
    os.chdir(path)
    try:
        yield
    finally:
        os.chdir(oldcwd)
